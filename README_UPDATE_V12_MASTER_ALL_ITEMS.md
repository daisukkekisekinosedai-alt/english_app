# にゃんにゃんイングリッシュ v12 master全アイテム版

## 変更内容

master アカウントだけは、着せ替えアイテムを **すべて使える** ようにしました。

### masterの挙動

- クローゼットの全アイテムを最初から使用可能
- 学習条件を満たしていなくても装備可能
- クローゼット画面では全アイテムを解放済みとして表示
- 進捗は 100% 表示

### 一般ユーザーの挙動

- 今まで通り、学習実績で解放
- 挙動は変更なし

## DB変更

なし（前回版と同じ）。

## Render反映

```powershell
git add .
git commit -m "allow master to use all closet items"
git push
```
